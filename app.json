{
  "name": "WeKnow_api",
  "description": "A social education system",
  "scripts": {
  },
  "env": {
    "DATABASE_URL": {
      "required": true
    },
    "JWT_SECRET": {
      "required": true
    },
    "ISSUER": {
      "required": true
    }
  },
  "formation": {
  },
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "https://github.com/heroku/heroku-buildpack-go"
    }
  ]
}
